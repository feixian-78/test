<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cl.dao.ShangpinrukuDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.cl.entity.ShangpinrukuEntity" id="shangpinrukuMap">
        <result property="dingdanbianhao" column="dingdanbianhao"/>
        <result property="shangpinmingcheng" column="shangpinmingcheng"/>
        <result property="shangpinfenlei" column="shangpinfenlei"/>
        <result property="guige" column="guige"/>
        <result property="shuliang" column="shuliang"/>
        <result property="gongyingshangmingcheng" column="gongyingshangmingcheng"/>
        <result property="shoujihaoma" column="shoujihaoma"/>
        <result property="rukuriqi" column="rukuriqi"/>
        <result property="cangguanyuanzhanghao" column="cangguanyuanzhanghao"/>
        <result property="cangguanyuanxingming" column="cangguanyuanxingming"/>
        <result property="beizhu" column="beizhu"/>
    </resultMap>

    <select id="selectListView"
		resultType="com.cl.entity.view.ShangpinrukuView" >

		SELECT  shangpinruku.* FROM shangpinruku  shangpinruku 	        
         <choose>
        <when test="ew != null and ew.sqlSegment != null and ew.sqlSegment != '' and ew.sqlSegment.contains('WHERE')">
            <!-- 当SQL片段包含WHERE时，直接使用 -->
            ${ew.sqlSegment}
        </when>
        <when test="ew != null and ew.sqlSegment != null and ew.sqlSegment != '' and !ew.sqlSegment.toUpperCase().startsWith(' ORDER')">
            <!-- 当不包含WHERE且不以ORDER开头时，加WHERE -->
            WHERE ${ew.sqlSegment}
        </when>
        <when test="ew != null and ew.sqlSegment != null and ew.sqlSegment != ''">
            <!-- 其他情况（以ORDER开头）直接使用 -->
            ${ew.sqlSegment}
        </when>
    </choose>
	</select>
	
	<select id="selectView"
		resultType="com.cl.entity.view.ShangpinrukuView" >
		SELECT * FROM shangpinruku  shangpinruku  <choose>
        <when test="ew != null and ew.sqlSegment != null and ew.sqlSegment != '' and ew.sqlSegment.contains('WHERE')">
            <!-- 当SQL片段包含WHERE时，直接使用 -->
            ${ew.sqlSegment}
        </when>
        <when test="ew != null and ew.sqlSegment != null and ew.sqlSegment != '' and !ew.sqlSegment.toUpperCase().startsWith(' ORDER')">
            <!-- 当不包含WHERE且不以ORDER开头时，加WHERE -->
            WHERE ${ew.sqlSegment}
        </when>
        <when test="ew != null and ew.sqlSegment != null and ew.sqlSegment != ''">
            <!-- 其他情况（以ORDER开头）直接使用 -->
            ${ew.sqlSegment}
        </when>
    </choose>
	</select>



</mapper>
